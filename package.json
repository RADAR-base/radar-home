{
  "name": "radar-home",
  "version": "0.1.0",
  "description": "Home page for RADAR-base",
  "scripts": {
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -R src/* dist/",
    "build": "npm run copy && npx tailwindcss build -i src/styles.css -o dist/styles.css"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RADAR-base/radar-home.git"
  },
  "keywords": [
    "radar-base"
  ],
  "author": "Alexey Khleskov, Joris Borgdorff <joris@thehyve.nl>",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/RADAR-base/radar-home/issues"
  },
  "homepage": "https://github.com/RADAR-base/radar-home#readme",
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  }
}
